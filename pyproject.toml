[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "vampires_dpp"
description = "VAMPIRES Data Processing Pipeline"
readme = "README.md"
license = {file = "LICENSE"}
authors = [{name = "Miles Lucas", email = "mileslucas@arizona.edu"}]
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = [
    "amical@git+https://github.com/SAIL-Labs/AMICAL@vampires_dev",
    "annotated_types",
    "astropy",
    "astroquery>=0.4.8",
    "astroscrappy",
    "bottleneck",
    "click",
    "hcipy",
    "image-registration",
    "loguru",
    "matplotlib",
    "numpy>=2",
    "opencv_python",
    "pandas",
    "photutils",
    "pydantic",
    "reproject",
    "result",
    "rich",
    "scikit_image",
    "scipy",
    "sep",
    "synphot",
    "tomli_w",
    "tomli",
]

[project.urls]
homepage = "https://github.com/scexao-org/vampires_dpp"
repository = "https://github.com/scexao-org/vampires_dpp"
documentation = "https://scexao-org.github.io/vampires_dpp/"

[project.optional-dependencies]
dev = ["ty", "ruff"]
test = [
    "pytest",
    "pytest_cov",
    "pytest_randomly",
]
docs = [
    "proplot",
    "sphinx",
    "myst_nb",
    "sphinx_book_theme",
    "sphinxcontrib-eval",
    "sphinx_autodoc_typehints",
    "zenodo_get",
    "sphinx_click",
]

[project.scripts]
dpp = "vampires_dpp.cli.main:main"

[tool.hatch.version]
path = "src/vampires_dpp/__init__.py"

[tool.hatch.metadata]
allow-direct-references = true

[tool.pytest.ini_options]
log_cli = false
log_cli_level = "DEBUG"
log_cli_format = "[%(levelname)s] (%(name)-15s) %(message)s"
addopts = "--randomly-seed=4796"

[tool.coverage.run]
source = ["vampires_dpp"]

[tool.ruff]
line-length = 100

[tool.ruff.format]
skip-magic-trailing-comma = true

[tool.ruff.lint]
# pycodestyle, pydocstyle, isort, pyupgrade, pylint, tidyimports, use-pathlib, numpy, errmsg
select = ["E", "F", "B", "SIM", "I", "UP", "TID", "PTH", "NPY", "EM", "NPY201"]
ignore = [
    "E501", # line too long
]

[tool.ruff.lint.flake8-tidy-imports]
ban-relative-imports = "parents"

[tool.ruff.lint.isort]
split-on-trailing-comma = false

[tool.ruff.lint.pydocstyle]
convention = "numpy"

[tool.ty.environment]
python-version = "3.10"
